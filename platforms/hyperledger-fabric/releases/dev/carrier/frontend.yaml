apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: carrier-frontend
  namespace: carrier-net
  annotations:
    flux.weave.works/automated: "false"
spec:
  chart:
    path: examples/supplychain-app/charts/frontend
    git: "ssh://git@github.com/c-dineshkumar/blockchain-automation-framework.git"
    ref: "develop"
  releaseName: carrierfabric-frontend
  values:
    nodeName: carrier-frontend
    metadata:
      namespace: carrier-net
    replicaCount: 1
    frontend:
      serviceType: ClusterIP
      nodePorts:
        port: 8080
        targetPort: 80
      image: index.docker.io/hyperledgerlabs/supplychain_frontend:latest
      pullPolicy: Always
      pullSecrets: regcred
      env:
        webserver: https://carrierapi.org3ambr.fab.cluster4.aws.bafevent.com
    deployment:
      annotations: {}
    proxy:
      provider: haproxy
      peer_name: carrier
      external_url_suffix: org3ambr.fab.cluster4.aws.bafevent.com
      ambassador_secret: peer0-carrier-net-ambassador-certs
